let cards = [7, 4]
cards.push(6)
console.log(cards)

//Exercise : Push  the newMessage to the messages array,and then log out the array.
let message = [
    "hey, how are you?",
    "I am great, thankyou! How about you?",
    "All good. Been working on 100DaysOfCode today"
]

let newMessage = "That's Great!"
message.push(newMessage)
console.log(message)
message.pop() //to remove the last item in the arrays
console.log(message)